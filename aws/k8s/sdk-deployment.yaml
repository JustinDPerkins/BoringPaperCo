apiVersion: apps/v1
kind: Deployment
metadata:
  name: sdk
  namespace: boring-paper-co
  labels:
    cloud-provider: aws
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sdk
  template:
    metadata:
      labels:
        app: sdk
        cloud-provider: aws
    spec:
      containers:
      - name: sdk
        image: 975050242527.dkr.ecr.us-west-2.amazonaws.com/boringpaperco/sdk:latest
        ports:
        - containerPort: 5000
        env:
        - name: API_KEY
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: API_KEY
        - name: REGION
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: REGION
        envFrom:
        - configMapRef:
            name: app-config
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: sdk-service
  namespace: boring-paper-co
  labels:
    cloud-provider: aws
spec:
  selector:
    app: sdk
  ports:
  - port: 5000
    targetPort: 5000
  type: ClusterIP 