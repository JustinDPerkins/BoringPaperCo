apiVersion: apps/v1
kind: Deployment
metadata:
  name: ui
  namespace: boring-paper-co
  labels:
    cloud-provider: aws
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ui
  template:
    metadata:
      labels:
        app: ui
        cloud-provider: aws
    spec:
      containers:
      - name: ui
        image: 975050242527.dkr.ecr.us-west-2.amazonaws.com/boringpaperco/ui:latest
        ports:
        - containerPort: 80
        envFrom:
        - configMapRef:
            name: app-config
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: ui-service
  namespace: boring-paper-co
  labels:
    cloud-provider: aws
spec:
  selector:
    app: ui
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP 