apiVersion: apps/v1
kind: Deployment
metadata:
  name: containerxdr
  namespace: boring-paper-co
  labels:
    cloud-provider: aws
spec:
  replicas: 1
  selector:
    matchLabels:
      app: containerxdr
  template:
    metadata:
      labels:
        app: containerxdr
        cloud-provider: aws
    spec:
      containers:
      - name: containerxdr
        image: 975050242527.dkr.ecr.us-west-2.amazonaws.com/boringpaperco/containerxdr:latest
        ports:
        - containerPort: 8081
        envFrom:
        - configMapRef:
            name: app-config
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
            add:
            - CHOWN
            - SETGID
            - SETUID
            - NET_BIND_SERVICE
          runAsNonRoot: true
          runAsUser: 1000
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: containerxdr-service
  namespace: boring-paper-co
  labels:
    cloud-provider: aws
spec:
  selector:
    app: containerxdr
  ports:
  - port: 8081
    targetPort: 8081
  type: ClusterIP 